# Todos
[ ] Migrate the entire codebase to TypeScript. This involves installing dependencies, configuring tsconfig, and converting all necessary files (.jsx -> .tsx). This should be done before building new features.
[ ] Setup project routing using React Router for main routes: /, /about, /blog, /gallery.
[ ] Build core structural components: Header (must include all main links) and Footer.
[ ] Implement static content pages: Home (/, containing the carousel), About (short resume).
[ ] Develop the PhotoGallery component and integrate the Carousel logic for the Home page.
[ ] Implement Blog List and individual Blog Post components to display content from markdown files.