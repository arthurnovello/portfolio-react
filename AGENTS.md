# Project Conventions & Workflow Guide

This repository is a React portfolio built with Vite. Follow these specific guidelines for development and task execution.

## 🚀 Development Workflow
*   **Start Server:** Use `npm run dev`. The development server runs on port `3000` and automatically opens the browser.
*   **Build:** The production build command is `npm run build`.
*   **Preview:** After building, serve the artifacts using `npm run preview`.

## ⚙️ Tooling & Architecture
*   **Bundler/Framework:** Uses Vite and `@vitejs/plugin-react`.
*   **Source Code Location:** All application logic resides in the `src/` directory.
*   **Structure:** Standard React component structure.

## ✅ Verification
*   **Testing:** (No explicit testing commands found, assume standard React testing setup if needed).
*   **Linting/Typechecking:** (No explicit lint or typecheck commands found. Agent should verify if these are needed for a specific task).